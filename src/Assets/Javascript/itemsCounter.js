export const countItems = (array) => {
  let count = 0;
  for (let i = 0; i < array.length; i += 1) {
    count += 1;
  }
  return count;
};

export const countComments = (array) => {
  let count = 0;
  for (let i = 0; i < array.length; i += 1) {
    count += 1;
  }
  return count;
};